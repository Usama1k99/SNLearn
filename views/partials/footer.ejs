    <!-- Interactive Cursor Elements -->
    <div class="cursor-ghost"></div>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p class="footer-text">ServiceNow Developer Learning Hub — Built for continuous learning</p>
            </div>
        </div>
    </footer>

    <script src="/scripts/global.js"></script>
    <% if (typeof scripts !== 'undefined') { %>
        <% scripts.forEach(function(script) { %>
            <script src="<%= script %>"></script>
        <% }); %>
    <% } %>
</body>
</html>
